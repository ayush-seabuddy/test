build:
  name: Create production builds
  triggers:
    - event: push
      branch: feature/expo-migration
  steps:
    - eas/checkout
    - eas/install_dependencies
    - eas/build:
        platform: all
        profile: preview
        auto_submit: true
    - eas/submit:
        platform: all
        profile: preview
